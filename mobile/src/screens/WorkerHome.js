import React from 'react';
import { View, Button } from 'react-native';

export default function WorkerHome({ navigation }) {
  return (
    <View>
      <Button title="View Today's Salary" onPress={() => {}} />
      <Button title="View Salary Calendar" onPress={() => {}} />
      <Button title="View Salary by Date Range" onPress={() => {}} />
      <Button title="Change Password" onPress={() => {}} />
      {/* Implement actual fetch and navigation */}
    </View>
  );
}
